# Build artifacts
target/
*.exe
*.pdb

# Local claim-run output
*.log

# LaTeX build artifacts
paper.aux
paper.log
paper.out
paper.synctex.gz
paper.toc
paper.bbl
paper.blg

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local v0.13.0 practice-run state and publication journals. Credentials are
# held by Git Credential Manager and must never be written below this tree.
.xcelerator-local/

# `data/zeta_zeros.json` is tracked reference data. Runtime cache artifacts
# belong under the toolkit-managed cache root, not under `data/`.
