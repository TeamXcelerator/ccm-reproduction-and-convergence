# Build artifacts
target/
*.exe
*.pdb

# Local claim-run output
*.log

# LaTeX build artifacts
paper.aux
paper.log
paper.out
paper.synctex.gz
paper.toc
paper.bbl
paper.blg

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local toolkit practice-run state and publication journals. Credentials are
# held by Git Credential Manager and must never be written below this tree.
.xcelerator-local/

# Local cache-output validation artifacts and comparison reports.
.xcelerator-cache/
cache-validation/
xcelerator-validation/
.xcelerator-cache-validation/
.xcelerator-validation/

# Local performance reports. These contain machine- and run-specific timing
# observations and are never paper evidence or cache artifacts.
benchmark-results/
*.benchmark.json

# Toolkit-internal stage timing sidecars.
performance-reports/
*.performance.json

# Legacy GL node cache when XC_CACHE_ROOT is unset (see xc-numerics quadrature).
/data/
